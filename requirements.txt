# Runtime dependencies for bike-plate-blur
# Install PyTorch first (CPU or CUDA) — see README.md — then:
#   pip install -r requirements.txt

ultralytics>=8.3.0
sahi>=0.11.0
opencv-python>=4.8.0
numpy>=1.24.0
tqdm>=4.66.0
tomli>=2.0.0; python_version < "3.11"
